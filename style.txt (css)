<style>
        Estilos Gerais e Fundo Suave
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: #333;
        }

        /* Container Principal (Card) */
        .card {
            background-color: #ffffff;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 90%;
            text-align: center;
        }

        h1 {
            font-size: 1.9rem;
            margin-bottom: 1rem;
            color: #ff0000;
        }

        p {
            font-size: 0.95rem;
            color: #666;
            margin-bottom: 1.5rem;
            color: blue;
        }

        /* Área de Interação */
        .controls {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }

        input[type="color"] {
            border: none;
            width: 100%;
            height: 40px;
            cursor: pointer;
            border-radius: 4px;
            background: none;
        }

        /* A Caixa Colorida */
        #box {
            width: 100%;
            height: 150px;
            background-color: #ffffff;
            border: 2px solid #ddd;
            border-radius: 8px;
            margin-bottom: 1rem;
            transition: background-color 0.3s ease;
        }

        /* Texto do Código da Cor */
        #colorCode {
            font-family: monospace;
            font-size: 1rem;
            font-weight: bold;
            color: #555;
            display: block;
        }
    </style>